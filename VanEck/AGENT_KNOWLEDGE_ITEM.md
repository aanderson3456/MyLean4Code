# Optimal Lean 4 Formalization Style & Workflow (KI Parameters)

This matrix formally defines the coding parameters and persona exactly required to seamlessly execute continuous verification bounds natively over the Lean 4 compiler for all subsequent workspace sequences.

### 1. Zero Rabbit Holes
Mathematical proof boundaries must rigidly map across exact topologies exactly isolated within their local subsets. If any geometric limit boundary spins more than $n=4$ `sorry` traces into $2n$ loops, the structural bindings are poorly mapped. 
- **Action**: Stop and mechanically step back logically. Never construct complex infinite sequences dynamically without breaking them linearly!

### 2. The 50-Line Limit
Do not construct generalized abstract limit theorems exceeding 50 lines.
- **Action**: Slice the problem limits up strictly passing parameters directly down through mathematical subset boundary proofs statically. Wrap bounds independently (e.g. tracking topological subsets natively avoiding deep math tree searches).

### 3. Topological Proxy Variables
Mathlib limits (especially Carathéodory's variations tracking dimension metrics or OrderTop spaces natively resolving `ENNReal`) aggressively infer completely arbitrary generic infinity limits throwing `?m` errors dynamically.
- **Action**: Do NOT fight Mathlib's metric sequence topology directly in one monolithic step! Safely proxy your boundaries first! Construct explicit parameter constants tracking exactly to real arrays natively (i.e. `if r = \top then 1 else r.toReal`), execute your bounded finite logic exactly to subset constraints correctly, and finally apply `tsum_le_sum` / `subsingleton` dynamically bridging zero! 

### 4. Curriculum Target
Ensure outputs match logically structurally perfectly against Mathgod format targets, executing natively without causing scope errors spanning dependencies. Proof limits will eventually be extracted mathematically onto mathgod.org natively mapping tracking exercises linearly for K-6 / K-12 math modules perfectly.
